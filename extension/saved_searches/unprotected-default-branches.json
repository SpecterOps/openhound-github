{
  "name": "[GitHub] Unprotected Default Branches",
  "query": "MATCH p=(repo:GH_Repository)-[:GH_HasBranch]-(branch:GH_Branch {protected: false}) WHERE repo.default_branch = branch.short_name RETURN p LIMIT 1000",
  "description": "Returns all default branches in repositories that are not protected."
}
